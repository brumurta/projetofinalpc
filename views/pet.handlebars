<div class="container padding-top">
  <div class="row justify-content-center">
    <div class="col-md-6 esticar">
      <div class="card shadow">
        <div class="card-body">
          <h1 class="centralizar">Lista dos Pets</h1>
          <table class="table">
            <thead>
              <tr>
                <th>id</th>
                <th>nome</th>
                <th>Dono</th>
                <th>Raça</th>
                <th>Idade</th>
                <th>Peso</th>
              </tr>
            </thead>
            {{#each listarPet}}
              <tbody>
                <tr>
                  <td>{{id}}</td>
                  <td>{{nome_pet}}</td>
                  <td>{{nome_dono}}</td>
                  <td>{{raca}}</td>
                  <td>{{idade}}</td>
                  <td>{{peso}}</td>
                  <td><a
                      type="button"
                      class="btn btn-success"
                      href="/controlepet/editpet/{{id}}"
                    >Editar</a>
                  </td>
                  <td><a
                      type="button"
                      class="btn btn-danger"
                      href="/controlepet/removepet/{{id}}"
                    >excluir</a></td>
                </tr>
              </tbody>
            {{/each}}
          </table>
        </div>
      </div>
    </div>
  </div>
</div>